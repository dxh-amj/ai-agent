import { PricingPage } from "@/modules/pricing";

export const metadata = {
  title: "Pricing - AI Workforce Hub",
  description:
    "Transparent pricing for every stage. Scale your AI workforce without breaking the bank.",
};

const Pricing = () => {
  return <PricingPage />;
};

export default Pricing;
